import * as React from "react";
import { Languages } from "../App";

export const Invitation: React.FunctionComponent<{ language: Languages }> = ({
  language
}) => {
  return <div id="invitation">Invitation stuff</div>;
};
